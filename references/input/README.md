# Visual Reference Inputs

Place 2-3 reference images here and add paths to config:

```yaml
reference_images:
  - ../references/input/ref_01.jpg
  - ../references/input/ref_02.jpg
```

Notes:

- Use the same character/style anchors you want to preserve.
- Keep file names stable across iterations.
